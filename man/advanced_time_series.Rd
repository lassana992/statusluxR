% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advanced_time_series.R
\name{advanced_time_series}
\alias{advanced_time_series}
\title{Create an Advanced and Interactive Time Series Plot}
\usage{
advanced_time_series(
  data,
  date_var,
  y_var,
  group_var = NULL,
  aggregation = c("none", "daily", "weekly", "monthly", "quarterly", "yearly"),
  agg_func = c("mean", "sum"),
  smooth_method = c("none", "loess", "lm", "gam"),
  theme_choice = c("minimal", "classic", "bw", "light", "gray", "void"),
  palette = "Set1",
  manual_colors = NULL,
  line_size = 0.8,
  line_type = c("solid", "dashed", "dotted"),
  show_points = TRUE,
  point_size = 1.5,
  alpha = 1,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  xlab = NULL,
  ylab = NULL,
  legend_title = NULL,
  legend_position = "right",
  font_size = 12,
  font_family = "sans",
  interactive = FALSE,
  export = FALSE,
  export_format = c("png", "jpg", "pdf", "svg", "tiff"),
  file_name = "time_series_plot",
  width = 10,
  height = 5,
  dpi = 300
)
}
\arguments{
\item{data}{A data frame containing the dataset.}

\item{date_var}{The date or date-time variable (unquoted).}

\item{y_var}{The numerical variable to plot on the y-axis (unquoted).}

\item{group_var}{Optional categorical grouping variable for multiple series (unquoted).}

\item{aggregation}{Character. Temporal aggregation level: \code{"none"}, \code{"daily"}, \code{"weekly"}, \code{"monthly"}, \code{"quarterly"}, or \code{"yearly"}.}

\item{agg_func}{Character. Summary function for aggregation: \code{"mean"} or \code{"sum"}.}

\item{smooth_method}{Character. Trend line smoothing method: \code{"none"}, \code{"loess"}, \code{"lm"}, or \code{"gam"}.}

\item{theme_choice}{Character. Plot theme: \code{"minimal"}, \code{"classic"}, \code{"bw"}, \code{"light"}, \code{"gray"}, or \code{"void"}.}

\item{palette}{Character. Built-in discrete color palette (e.g., \code{"Set1"}, \code{"Dark2"}, \code{"viridis"}, \code{"plasma"}).}

\item{manual_colors}{A character vector of exact colors to map to groups (overrides \code{palette}).}

\item{line_size}{Numeric. Thickness of the time series lines (default 0.8).}

\item{line_type}{Character. Style of the line: \code{"solid"}, \code{"dashed"}, or \code{"dotted"}.}

\item{show_points}{Logical. If TRUE, adds point markers along the lines.}

\item{point_size}{Numeric. Size of the point markers.}

\item{alpha}{Numeric. Line and point transparency (0 to 1).}

\item{title, subtitle, caption, xlab, ylab, legend_title}{Character strings for plot text.}

\item{legend_position}{Character. \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}, or \code{"none"}.}

\item{font_size}{Numeric. Base font size for the plot layouts.}

\item{font_family}{Character. Font family (e.g., \code{"sans"}, \code{"serif"}, \code{"mono"}).}

\item{interactive}{Logical. If TRUE, converts the static plot to an interactive plotly object.}

\item{export}{Logical. If TRUE, saves the static plot to the working directory.}

\item{export_format}{Character. \code{"png"}, \code{"jpg"}, \code{"pdf"}, \code{"svg"}, or \code{"tiff"}.}

\item{file_name}{Character. The base name for the exported file.}

\item{width, height, dpi}{Numeric. Dimensions and resolution for the exported file.}
}
\value{
A \code{ggplot} object, or a \code{plotly} object if \code{interactive = TRUE}.
}
\description{
Generates publication-quality time series visualizations from raw or aggregated data,
featuring dynamic date truncation, smoothing trends, and optional interactive output.
}
