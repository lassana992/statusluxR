% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_engine.R
\name{pivot_engine}
\alias{pivot_engine}
\title{Advanced R Pivot Table Engine}
\usage{
pivot_engine(
  data,
  rows,
  cols = NULL,
  values,
  agg = c("sum", "mean", "median", "count", "min", "max", "sd", "var"),
  na_rm = TRUE,
  fill_value = 0,
  format = c("wide", "long", "plot", "gt"),
  plot_type = c("bar", "heatmap", "line"),
  export_format = c("none", "csv", "excel"),
  file_name = "pivot_summary"
)
}
\arguments{
\item{data}{A data frame.}

\item{rows}{Character vector. Columns to group by on the rows.}

\item{cols}{Character vector. Columns to pivot across the top (optional).}

\item{values}{Character vector. Numeric columns to aggregate.}

\item{agg}{Character. Aggregation method: "sum", "mean", "median", "count", "min", "max", "sd", "var".}

\item{na_rm}{Logical. Should NA values be removed before aggregation? Default TRUE.}

\item{fill_value}{Numeric. Value to replace NAs with in the wide pivot format. Default 0.}

\item{format}{Character. Output format: "wide" (Excel style), "long" (ggplot style), "plot" (auto-visualize), or "gt" (beautiful static table).}

\item{plot_type}{Character. If format="plot", choose "bar", "line", or "heatmap".}

\item{export_format}{Character. Optional export: "none", "csv", or "excel".}

\item{file_name}{Character. Base file name for export.}
}
\value{
A data frame, tibble, ggplot object, or gt table depending on the format chosen.
}
\description{
Dynamically aggregates, reshapes, and visualizes datasets in an Excel-style pivot format.
}
